## React Context API (useContext Hook) – Simple Explanation

The Context API in React is used to share data globally across components without passing props manually at every level (prop drilling).

🔹 Why use Context API?

Without Context:

App → ComponentA → ComponentB → ComponentC


You must pass props through every component even if only ComponentC needs it.

Context API solves this problem.

🔹 When should you use it?

Use Context API for:

User authentication data

Theme (dark / light mode)

Language settings

Cart data (e-commerce)

Logged-in user info

❌ Avoid for very frequently changing data (use Redux/Zustand instead).

🔹 Main parts of Context API

createContext() – creates context

Provider – provides data

useContext() – consumes data

🔹 Step-by-Step Example
1️⃣ Create Context
// UserContext.js
import { createContext } from "react";

const UserContext = createContext();

export default UserContext;

2️⃣ Provide Context (Wrap App)
// App.js
import UserContext from "./UserContext";

function App() {
  const user = {
    name: "Abhi",
    role: "Admin"
  };

  return (
    <UserContext.Provider value={user}>
      <Dashboard />
    </UserContext.Provider>
  );
}

export default App;

3️⃣ Consume Context using useContext
// Dashboard.js
import { useContext } from "react";
import UserContext from "./UserContext";

function Dashboard() {
  const user = useContext(UserContext);

  return (
    <div>
      <h2>Welcome {user.name}</h2>
      <p>Role: {user.role}</p>
    </div>
  );
}

export default Dashboard;

🔹 Using Context with State (Real-world Example)
// ThemeContext.js
import { createContext, useState } from "react";

export const ThemeContext = createContext();

export function ThemeProvider({ children }) {
  const [theme, setTheme] = useState("light");

  return (
    <ThemeContext.Provider value={{ theme, setTheme }}>
      {children}
    </ThemeContext.Provider>
  );
}


Consume it:

import { useContext } from "react";
import { ThemeContext } from "./ThemeContext";

function ThemeButton() {
  const { theme, setTheme } = useContext(ThemeContext);

  return (
    <button onClick={() => setTheme(theme === "light" ? "dark" : "light")}>
      Current Theme: {theme}
    </button>
  );
}

🔹 Advantages

✔ Avoids prop drilling
✔ Clean and readable code
✔ Built-in (no external library)

🔹 Limitations

❌ Re-renders all consumers on value change
❌ Not ideal for complex state management

🔹 Context API vs Redux
Feature	Context API	Redux
Built-in	✅	❌
Boilerplate	Low	High
Performance	Medium	High
Best for	Small–Medium apps	Large apps
🔹 Summary

useContext helps access shared data easily

Use it for global app state

For complex apps → combine Context + Reducer or Redux